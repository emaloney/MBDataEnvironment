language: objective-c
xcode_workspace: MBDataEnvironment.xcworkspace
osx_image: xcode8

git:
  depth: 1
  submodules: false

before_install:
  git submodule update --init

install:
  gem install xcpretty

before_script:
  sudo systemsetup -settimezone America/New_York

script:
- set -o pipefail && xcodebuild -workspace MBDataEnvironment.xcworkspace -scheme 'MBDataEnvironment-iOS' -destination 'platform=iOS Simulator,OS=10.0,name=iPhone 7' -destination-timeout 120 clean build | xcpretty
- set -o pipefail && xcodebuild -workspace MBDataEnvironment.xcworkspace -scheme 'MBDataEnvironment-iOS' -destination 'platform=iOS Simulator,OS=10.0,name=iPhone 7' -destination-timeout 120 test | xcpretty
- set -o pipefail && xcodebuild -workspace MBDataEnvironment.xcworkspace -scheme 'MBDataEnvironment-macOS' -destination 'platform=macOS' -destination-timeout 120 clean build | xcpretty
- set -o pipefail && xcodebuild -workspace MBDataEnvironment.xcworkspace -scheme 'MBDataEnvironment-macOS' -destination 'platform=macOS' -destination-timeout 120 test | xcpretty
